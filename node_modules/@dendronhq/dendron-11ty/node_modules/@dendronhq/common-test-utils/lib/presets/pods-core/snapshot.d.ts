import { TestPresetEntry } from "../../utils";
declare const SNAPSHOT_TEST_PRESETS: {
    EXPORT: {
        DEFAULTS: TestPresetEntry<{
            vaultDir: string;
        }, unknown, {
            wsRoot: string;
            snapshotDirPath: string;
        }>;
    };
    IMPORT: {
        BASIC: TestPresetEntry<{
            vaultDir: string;
        }, unknown, {
            vaultDirPath: string;
        }>;
    };
};
export default SNAPSHOT_TEST_PRESETS;
