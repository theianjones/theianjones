import { DNoteRefData, DNoteRefLink, WorkspaceOpts } from "@dendronhq/common-all";
import { DendronEngineV2 } from "../../../../enginev2";
export declare function createRefLink({ fname, ...data }: {
    fname: string;
} & DNoteRefData): DNoteRefLink;
export declare const createEngine: ({ vaults, wsRoot }: WorkspaceOpts) => DendronEngineV2;
