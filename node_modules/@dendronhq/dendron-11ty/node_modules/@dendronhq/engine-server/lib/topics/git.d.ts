import execa from "execa";
declare type GitOpts = {
    cwd: string;
};
export declare class GitV2 {
    static clone(cmd: string, opts?: GitOpts): Promise<execa.ExecaReturnValue<string>>;
}
export declare class Git {
    opts: {
        localUrl: string;
        remoteUrl?: string;
    };
    static getRepo(fpath: string): Promise<any | false>;
    static createRepo(fpath: string, opts?: {
        initCommit: boolean;
    }): Promise<void>;
    constructor(opts: {
        localUrl: string;
        remoteUrl?: string;
    });
    isRepo(): Promise<boolean>;
    clone(destOverride?: string): Promise<string>;
    addAll(): Promise<void>;
    commit(opts: {
        msg: string;
    }): Promise<void>;
    push(): Promise<void>;
}
export {};
