import { DVault } from "./typesv2";
export declare class WorkspaceUtilsCommon {
    static getRepoDir(wsRoot: string): string;
    static getPathForVault(opts: {
        vault: DVault;
        wsRoot: string;
    }): string;
}
