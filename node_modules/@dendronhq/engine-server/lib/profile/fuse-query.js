"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_extra_1 = __importDefault(require("fs-extra"));
const enginev2_1 = require("../enginev2");
// if (mode === "schema") {
//     this.fuseEngine.updateSchemaIndex(this.schemas);
//   } else {
//     this.fuseEngine.updateNotesIndex(this.notes);
//   }
async function main() {
    const engine = enginev2_1.DendronEngineV2.createV3({ vaults: [], wsRoot: __dirname });
    console.log(__dirname);
    const notes = (await fs_extra_1.default.readJSON("/tmp/notes.json"));
    engine.store.notes = notes;
    await engine.updateIndex("note");
    const resp = await engine.queryNotes({ qs: "pr." });
    const preview = resp.data.slice(0, 50).map((ent) => ent.fname);
    console.log(JSON.stringify(preview, null, 4));
    console.log("done");
}
main();
//# sourceMappingURL=fuse-query.js.map