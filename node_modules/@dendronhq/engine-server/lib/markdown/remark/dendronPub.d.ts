import Unified, { Transformer } from "unified";
declare type TransformerOpts = {
    assetsPrefix?: string;
};
declare type PluginOpts = TransformerOpts;
declare function plugin(this: Unified.Processor, opts?: TransformerOpts): Transformer;
export { plugin as dendronPub };
export { PluginOpts as DendronPubOpts };
