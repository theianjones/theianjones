import { ConfigGetPayload, ConfigWriteOpts, RespV2 } from "@dendronhq/common-all";
import { WorkspaceRequest } from "@dendronhq/common-server";
export declare class ConfigController {
    static singleton?: ConfigController;
    static instance(): ConfigController;
    get({ ws }: WorkspaceRequest): Promise<RespV2<ConfigGetPayload>>;
    write({ ws, ...opts }: WorkspaceRequest & ConfigWriteOpts): Promise<RespV2<void>>;
}
