import { DVault } from "@dendronhq/common-all";
import { DendronAPI } from "@dendronhq/common-server";
declare type TestCb = (opts: {
    wsRoot: string;
    vaults: DVault[];
    api: DendronAPI;
}) => Promise<void>;
export declare function runTest(cb: TestCb): Promise<void>;
export {};
